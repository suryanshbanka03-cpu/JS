//concatenating
const name="suryansh"
const repocount =20
console.log(`hellomy name is ${name} and myrepo count is ${repocount}`)
//another way of declaring string
const gameName = new String('surya')
//console.log(gameName[0])
//console.log(gameName.__proto__);
//console.log(gameName.length)
//console.log(gameName.toUpperCase())
//console.log(gameName.charAt(2));
const s= gameName.substring(0,4)
//in slice function we can write it as string.slice(-8,4), in this case string starts reading backward if length is 8
//trim used to cut space
//split function splits the string on the basis of seperator and gives the splitted elements as in an array
